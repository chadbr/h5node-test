## Code
- Clone the code:
`git clone https://github.com/chadbr/_git/hdf5node-test`
- Install yarn - https://yarnpkg.com/en/docs/install
- Run `yarn install`
- Run `yarn build`

## Devlopment

- Build `yarn build`
- Run `yarn start`
